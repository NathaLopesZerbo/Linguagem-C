#include <string.h>
#include <stdio.h>

int main()
{
    int numInt; // printf("%d", a);
    double numDouble; // printf("%.2lf", b);
    char sexo;

    numInt = 10;
    numDouble = 1600.70;
    sexo = 

    printf("%d\n", numInt);
    printf("%.2lf\n", numDouble);

    return 0;
}